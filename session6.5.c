#include <stdio.h>
#include <math.h>
void main(){
	int a = 80;
	char b = 'c';
	printf("this is the number stored in 'a' %d\n",a);
	printf("this is a character interpreted form 'a' %c\n",a);
	printf("this is also a chareacter stored in 'b' %c\n",b);
	printf("Hey! the character of 'b' is printed as a number!%d\n",b);
	return 0;
}
